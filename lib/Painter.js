/* ************************************************
** GAME PLAYER CLASS
************************************************ */
var Painter = function (startId) {
  var id = startId

  // Define which variables and methods can be accessed
  return {    
    id: id
  }
}

// Export the Player class so you can use it in
// other files by using require("Player")
module.exports = Painter
